# Engineering Pass 2.6 — Future Extension Points

- Generate immutable ingredient indexes during the data-import pipeline.
- Generate compact search tokens or a prefix index at build time.
- Move advanced fuzzy or ranked queries to a Worker endpoint if needed.
- Add browser Performance API instrumentation for real-device startup and interaction timing.
- Add memory-pressure testing for low-memory Android devices.
- Add incremental data-index rebuilding when the Google update pipeline is implemented.
- Consider compact integer row identifiers if cloud-backed inventory queries become necessary.
