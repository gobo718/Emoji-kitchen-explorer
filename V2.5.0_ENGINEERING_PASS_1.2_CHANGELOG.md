# v2.5.0 Engineering Pass 1.2 — Repository Layer

## Modified

- `billy-repositories.js`
  - Centralized repository persistence keys.
  - Consolidated progress fallback and snapshot handling.
  - Consolidated required-cloud validation.
  - Expanded internal documentation and readability.
  - Added a read-only repository key registry.
  - Incremented repository API version from 4 to 5.

- `tests/repository-foundation.test.js`
  - Updated the expected repository API version from 4 to 5.

## Behavior

No intended user-facing or stored-data behavior changed. Existing persistence key strings, repository method names, method arguments, return shapes, sync registrations, and domain responsibilities remain intact.

## Verification

All existing root and Worker test suites pass.
