const fs=require('fs'),vm=require('vm'),path=require('path');const root=path.resolve(__dirname,'..');let remote=[];const ctx={window:{},console,Map,Date};ctx.window.window=ctx.window;vm.createContext(ctx);vm.runInContext(fs.readFileSync(path.join(root,'billy-sync-manager.js'),'utf8'),ctx);const m=ctx.window.BillySyncManager;m.register('favorites',{pull:async()=>remote,push:async()=>{remote=['a+b'];return remote}});if(m.getStatus('favorites').state!=='local')throw Error('Initial state wrong');m.markPending('favorites');if(m.getStatus('favorites').state!=='pending')throw Error('Pending state wrong');m.configure({enabled:true,userId:'device-1'});m.push('favorites').then(()=>{if(m.getStatus('favorites').state!=='synced')throw Error('Sync state wrong');console.log('sync manager tests passed')}).catch(e=>{console.error(e);process.exit(1)});
