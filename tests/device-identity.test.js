const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');
test('device identity is stable and anonymous',()=>{const data={};const context={window:{BillyStorage:{get:(k,f)=>k in data?data[k]:f,set:(k,v)=>(data[k]=v)}},crypto:{randomUUID:()=> '12345678-1234-1234-1234-123456789abc'},Uint8Array,Date,Math};context.window.crypto=context.crypto;vm.createContext(context);vm.runInContext(fs.readFileSync('billy-device-identity.js','utf8'),context);const a=context.window.BillyDeviceIdentity.ensure(),b=context.window.BillyDeviceIdentity.ensure();assert.equal(a.id,b.id);assert.match(a.id,/^dev_/);assert.equal(context.window.BillyDeviceIdentity.getMaskedId().includes('…'),true)});
