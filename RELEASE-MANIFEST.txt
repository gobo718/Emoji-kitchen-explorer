Billy Labs v2.2.6 — Progress Cloud Sync

Baseline: Billy Labs v2.2.5 Cloud Sync Framework

Included:
- Unified seen + favorites cloud progress endpoint
- Persistent per-repository batch queue
- Offline retry when the browser comes back online
- Local/cloud union merge for progress restoration
- Favorites endpoint retained for backward compatibility
- Diagnostics queue count
- Visible website version v2.2.6
- Updated tests and release documentation

Cloud sync remains disabled until an API URL, user ID, and enabled mode are explicitly configured.
